def sum (a,b)
{
  echo "the sum of ${a} and $ {b} is ${a+b}"
}

def sub (a,b)
{
  echo "the subtraction of ${a} and ${b} is ${a-b}"
}

def mul (a,b)
{
  echp "the product of ${a} and ${b} is ${a*b}"
}

def div (a,b)
{
  echo "the division of ${a} and ${b} is ${a/b}"
}
